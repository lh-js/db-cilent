# Mac 打包和运行指南

## 📦 打包步骤

### 1. 构建应用

首先需要构建主进程和渲染进程代码：

```bash
npm run build
```

或者分别构建：

```bash
# 构建主进程（TypeScript → JavaScript）
npm run build:main

# 构建渲染进程（React + Vite）
npm run build:renderer
```

### 2. 打包成 Mac 应用

#### 方法 1：打包成 DMG 和 ZIP（推荐）

```bash
npm run dist:mac
```

或者：

```bash
npm run dist
```

这会在 `release` 目录下生成：
- `Neurix DB Client-1.0.0.dmg` - Mac 安装包（可直接分发给其他用户）
- `Neurix DB Client-1.0.0-mac.zip` - 压缩包

#### 方法 2：只打包应用目录（不生成安装包）

```bash
npm run pack
```

这会在 `release/mac` 目录下生成 `.app` 应用包。

## 🚀 运行打包后的应用

### 方式 1：运行 DMG 安装包

1. 打开 `release/Neurix DB Client-1.0.0.dmg`
2. 将应用拖拽到 Applications 文件夹
3. 在 Applications 中找到 "Neurix DB Client" 并双击运行

### 方式 2：直接运行 .app 文件

如果使用 `npm run pack`：

```bash
open release/mac/Neurix\ DB\ Client.app
```

或者：

```bash
cd release/mac
open "Neurix DB Client.app"
```

### 方式 3：从终端运行

```bash
./release/mac/Neurix\ DB\ Client.app/Contents/MacOS/Neurix\ DB\ Client
```

## ⚠️ Mac 安全提示

首次运行时，Mac 可能会提示"无法打开，因为来自身份不明的开发者"：

### 解决方法：

1. **方法 1：系统设置中允许**
   - 打开"系统设置" → "隐私与安全性"
   - 找到被阻止的应用，点击"仍要打开"

2. **方法 2：右键打开**
   - 右键点击应用
   - 选择"打开"
   - 在弹出的对话框中点击"打开"

3. **方法 3：移除隔离属性（开发者用）**
   ```bash
   xattr -cr "release/mac/Neurix DB Client.app"
   ```

## 📝 完整打包流程示例

```bash
# 1. 清理旧的构建文件（可选）
rm -rf dist release

# 2. 安装依赖（如果还没安装）
npm install

# 3. 构建应用
npm run build

# 4. 打包成 Mac 应用
npm run dist:mac

# 5. 运行打包后的应用
open release/Neurix\ DB\ Client-1.0.0.dmg
```

## 🔍 故障排除

### 打包失败

如果遇到错误，尝试：

1. **清理并重新构建**
   ```bash
   rm -rf dist release node_modules
   npm install
   npm run build
   npm run dist:mac
   ```

2. **检查 electron-builder 版本**
   ```bash
   npm list electron-builder
   ```

3. **查看详细错误信息**
   ```bash
   npm run dist:mac -- --debug
   ```

### 应用无法运行

1. 检查应用是否在 `release` 目录下
2. 检查控制台错误信息
3. 确保所有依赖都已正确打包

## 📦 分发应用

打包完成后，你可以：

1. **DMG 文件**：直接发送给其他 Mac 用户，他们双击安装即可
2. **ZIP 文件**：解压后即可运行，适合不需要安装的场景

## 🎨 自定义图标（可选）

如果需要自定义应用图标：

1. 准备一个 `.icns` 格式的图标文件
2. 放在 `build/icon.icns`
3. 重新打包

如果没有图标文件，electron-builder 会使用默认图标。

